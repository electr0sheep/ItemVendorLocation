namespace ItemVendorLocation
{
    public enum DataSource : byte
    {
        GarlandTools = 1,
        Internal = 2,
    }

    public enum ResultsViewType : byte
    {
        Single = 1,
        Multiple = 2,
    }
}
